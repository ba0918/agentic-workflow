# greet 多言語対応の仕様

挨拶の文言を言語ごとに切り替える。承認済みで、まだ実装されていない。

- 対応する言語は en と ja である
- 言語は環境変数 `GREET_LANG` で選ぶ。未設定と未対応の値は en に落とす
- en の挨拶は `Hello`、ja の挨拶は `こんにちは` である
- 言語ごとの文言は greet.py 本体から言語別の資源ファイルに分ける
- 言語の追加は、greet.py を変えずに資源ファイルを 1 つ置くだけで済む形にする
