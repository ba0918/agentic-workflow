# Plan review corrections

First, the reviewer finds a typo, an omitted expected test file, and a missing check command. These
corrections do not change the plan's structure.

Later scenarios independently change: step dependencies, step order, a completion condition, the
Scope tree topology, and a referenced specification.
