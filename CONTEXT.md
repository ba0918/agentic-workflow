# 開発ワークフロー

このリポジトリの skill（`skills/ba0918-*/`）で開発を進めるときに使う言葉の定義。
二通り以上に読める言葉が出たら、brainstorm がその場で定義を確かめてここに足す。
同じ物を指す言葉が複数あるときは 1 つを選び、残りを「使わない」に挙げる。

## 主体

**メインセッション**:
人と直接対話している LLM のセッション
使わない: 親セッション, オーケストレータ

**reviewer**:
review を担当する subagent
使わない: レビュー担当, review agent

**修正役**:
implement と同じ契約で動き、見えている指摘とブランチを受け取る subagent
使わない: fixer, 修正担当

## 文書

**理念**:
利用者が自分の言葉で書いた、ワークフローの最上位の文書（`docs/principles.md`）
使わない: principles, 原則

**用語集**:
この文書（`CONTEXT.md`）
使わない: 用語の節, glossary

**仕様書**:
brainstorm の成果物で、人が承認してコミットした文書（`docs/spec/<名前>.md`）
使わない: spec, 仕様

**手順書**:
plan の成果物で、人が承認してコミットした文書（`docs/plans/<名前>.md`）
使わない: plan, 実装計画, 計画書

**承認**:
人が差分を読んで明示した同意。理念、用語集、仕様書、手順書ではコミットと同じ操作
使わない: 合意, OK

## brainstorm

**木**:
決めごとの全体。決めごとには前提があり、決めると枝分かれする
使わない: ツリー, 決定木

**ラウンド**:
前提が揃った質問を通し番号と推奨回答付きでまとめて出す 1 回分
使わない: ターン, 往復

**推奨回答**:
質問に添える LLM の読み。人が答えなかった質問の推奨回答は採用しない
使わない: 既定の答え, デフォルト

**途中経過**:
brainstorm の現在の状態を書いた `.agents/tmp/brainstorm-<仕様書の名前>.md`
使わない: 進捗ファイル, 記録, state

**人を止める 4 つの場面**:
作業の途中で人に判断を求めてよい場面の一覧（意味が足りない、不可逆や危険な対象、事故、進捗が無い）
使わない: Human boundary, 停止条件, 4 条件

## review

**指摘**:
review が見つけた問題 1 件
使わない: finding, 問題点

**指摘 JSON**:
あるブランチの指摘の現在の状態をまとめた 1 ファイル（`.agents/artifacts/reviews/<ブランチ名>.json`）
使わない: 指摘の表, 台帳, レビュー結果

**current-state**:
現在の状態を 1 ファイルに上書きする記録の形
使わない: スナップショット, ログ

**profile**:
対象の種類ごとの評価規則（Code、Document、Skill）
使わない: 評価規則, プロファイル

**観点**:
1 体の reviewer に割り当てる見る角度（品質、突き合わせ）
使わない: perspective, 視点

**重さ**:
指摘の深刻さ（`security`、`critical`、`warn`、`info`）
使わない: severity, 重要度

**対応**:
指摘をどう扱うかの提案（`auto_fix`、`fix_and_verify`、`human_judgment`、`record_only`）
使わない: action, アクション

**確かめ方**:
指摘が直ったかを判定する手段の提案
使わない: oracle, 検証方法

**強さ**:
review が対象にする重さの範囲（`standard`、`light`）
使わない: depth, レベル

**診断**:
人が review を直接呼ぶこと。差分や文書などの対象を評価して指摘を洗い出し、突き合わせる相手があればそれとも突き合わせる
使わない: 直接レビュー, 単独レビュー

## cycle

**往復**:
review の呼び出し 1 回。reviewer が何体でも 1
使わない: ラウンド, イテレーション, ループ回数

**フルレビュー**:
比較の基点からブランチの先端までの差分全体を、前回の指摘を見せずに評価するレビュー
使わない: 全体レビュー, まっさらなレビュー

**差分レビュー**:
前回のレビュー以降の差分と open の指摘だけを評価するレビュー
使わない: 増分レビュー, インクリメンタルレビュー

**見えている指摘**:
対応が `auto_fix` か `fix_and_verify` で open の指摘
使わない: 未対応の指摘, 修正対象

**比較の基点**:
フルレビューが差分を取る起点の commit
使わない: base, ベース

**終端報告**:
cycle の終了時に人へ見せる報告
使わない: 最終報告, サマリ

## investigate

**調査**:
人が述べた症状や疑問から出発し、読むだけで原因と影響を辿ること。指摘の形（重さ、対応、確かめ方）は持たない
使わない: 調べ物, 原因調査

## iterate

**小さいタスク**:
仕様書も手順書も要らないタスク。要求が一通りにしか読めない、仕様の判断が要らない、仕様書があれば矛盾しない、影響が読める、の 4 つを全部満たす物。ファイル数は問わない
使わない: 小さい修正, Small, 軽いタスク

**要求**:
iterate が受け取る指示。人の言葉に、メインセッションが対話で知っていること（名前の挙がったファイル、決まっている方針、cycle か iterate の直後ならその終端報告で見たこと）を補い、文脈を知らない実装役が読んで書ける自己完結の文章にした物
使わない: 追加指示, 指示, タスク

**影響が読める**:
判定役が変更する場所を全部列挙でき、その列挙が閉じていて（「他にもありうる」が残らない）、各所で何を変えるかを判断なしに言える状態。場所の単位はファイルで、作る予定のファイルも含む。ファイルの中で何を変えるかは列挙に添えるが、閉じているかはファイルで見る。数で切らない
使わない: 影響が小さい, スコープが小さい

**判定役**:
要求が小さいタスクかどうかを読むだけで判定する subagent。変更する場所の列挙と 4 つの条件の判定を返す。investigate の「調査」とは出発点（要求か、症状や疑問か）で呼び分ける
使わない: 調査役, 影響範囲の調査, scope check

**実装役**:
修正役と同じ契約で動き、見えている指摘の代わりに要求と判定役の列挙（仕様書があればそのパスも）を受け取る subagent
使わない: implementer, 実装担当
