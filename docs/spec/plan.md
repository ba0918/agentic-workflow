# plan の仕様

plan は、brainstorm で承認された仕様書を、実装の手順書に変えるステップです。
手順書は、人が読んで「何がどう変わるか」を判断できる文書であると同時に、次のステップ
（implement）がそのまま実行できる文書です。

全体の中での位置と、brainstorm から何を受け取り implement へ何を渡すかは
[workflow.md](workflow.md) にあります。ここでは plan の中で何が起きるかを説明します。

## 何をするステップか

plan は「決まったことを、順番に並べる」だけのステップです。新しいことは決めません。
仕様書に書いていないことが手順を書く途中で必要になったら、自分で埋めずに brainstorm へ
戻します。これは、手順書が「2 回目の設計」になって、人が承認していない設計が実装に
流れ込むのを防ぐためです。

plan が終わったとき、次のものができています。

- 人が読んで承認した手順書（`.agents/artifacts/plans/` に置かれる）
- 未完了の手順書の索引（`open-plans.json`）への登録

手順書ができても、implement は自動では始まりません。人が「実装して」と言って初めて動きます。

## 受け取るもの

plan は、brainstorm または人が名前を挙げた仕様書だけを入力にします。リポジトリの中を
探して「これが関係ありそう」と仕様書を自分で選ぶことはしません。

手順書を書き始める前に、次がそろっているかを確かめます。

- 作る物が、1 回の実装と検証で終わる 1 つの単位になっている
- 参照する仕様書のパスと、その内容の指紋（SHA-256 のハッシュ値）が分かっている
- 仕様書に、完了を確かめる方法と、人が判断する場面が書いてある
- 配布の仕方、動かし方、保存する状態、外部サービスとの入出力が決まっているか、
  無いと確認されている
- 決まっていないこと（未決定）が残っていない

1 つでも欠けていたら、草稿を書かずに、何が足りなくてどう困るかを人の言葉で説明し、
brainstorm へ戻します。

## 手順書に書くこと

手順書は 1 つの Markdown ファイルです。人が読む部分と AI が読む部分を分けません。
全体を人の言葉で書いたうえで、implement が機械的に読む箇所だけ、決まった書き方をします。

### 人が読んで判断できること

手順書だけを読んで、次が分かるように書きます。

- この手順書の目的と、利用者が得る結果
- 何を変えて、何を変えないか
- 各手順がなぜ必要か
- 外部への影響と、主なリスク
- 各手順の完了をどう示すか
- 人が判断する場面
- 失敗したらどこで止まるか

手順書の中に出てくる仕様書の参照には、「この参照が実際に何を求めているか」を添えます。
ファイル名だけを並べて説明の代わりにすることはしません。

### implement が機械的に読む箇所

implement は、次の箇所を決まった書き方から読み取ります。書き方が違うと、implement は
手順書を受け付けずに止まります。

| 箇所 | 書き方 | implement が何に使うか |
|---|---|---|
| 手順書の ID と版 | 冒頭に `**Plan ID:**` と `**Plan revision:**` | 証拠をどの手順書に結び付けるか |
| 参照する仕様書 | `**対象仕様:**` の下に、パス・指紋・該当する節の一覧 | 実行前に、仕様書が承認時から変わっていないことを確かめる |
| 変更してよい範囲 | `## 変更するもの` の下の、ファイルの木（`text` のコードブロック） | 範囲の外への変更を拒否する |
| 手順 | `## 実装手順` の下の `### 1.`、`### 2.` … | 1 つずつ順番に実行する |
| 各手順の完了の示し方 | 各手順の中に `**完了の示し方:**` と 3 種類のうち 1 つ | 種類に応じた証拠を要求する |
| 人が判断する場面 | 該当する手順の中に `**Human gates:**` と JSON | 実行の前後で人の判断を待つ |

参照する仕様書の書き方の例です。

```markdown
**対象仕様:**

- `docs/spec/implement.md`
  - 内容identity: `sha256:0123…abcd`
  - 該当する節: 「残っている作業があるとき」「証拠の残し方」
```

implement は、パスと指紋だけを機械的に読みます。該当する節は人のためのもので、
「この手順書が仕様書のどこに基づいているか」を人が辿れるようにします。

変更してよい範囲の書き方の例です。コメントや注釈は書きません（implement の読み取りが
止まります）。

````markdown
## 変更するもの

```text
skills/ba0918-implement/
  SKILL.md
  references/
    execution.md
  scripts/
    implement_runtime.py
```
````

### 完了の示し方

各手順は、完了をどう示すかを次の 3 種類から 1 つ選んで書きます。

| 書き方 | 何に使うか | implement が要求する証拠 |
|---|---|---|
| `**完了の示し方:** テストで示す` | 決まった入力に決まった出力を返すコード | 先に失敗するテストを書き、通ったこと |
| `**完了の示し方:** 作った物で示す` | AI に読ませる文章、設定ファイル | 決めた内容のファイルが存在し、形式の検査を通り、人が読んで OK と言ったこと |
| `**完了の示し方:** 外で確かめる` | 実機での動作確認、人が頼んだ実測 | 実行した結果を人が見て OK と言ったこと |

どれを選ぶかの目安は、[workflow.md](workflow.md) の「plan → implement」の節にあります。
AI が実際に走らせる確認（実測）は、人が頼んだときだけ「外で確かめる」として手順に入れます。
頼まれていないのに AI が実測の手順を足すことはしません。

### 各手順に書くこと

手順ごとに次を書きます。

- 目的と、仕様書のどの節に基づくか
- 前提（先に終わっているべき手順、先に決まっているべきこと）
- 変更してよいファイルの範囲（手順書全体の範囲の中で、さらに絞る場合）
- 完了の示し方（上の 3 種類から 1 つ）と、具体的に何を確かめるか
- 実装側に任せてよい選択（あれば）
- 止まる条件

実装側に任せてよい選択は、どれを選んでも承認された振る舞いが変わらない範囲に限ります。
新しい入力の種類、受け入れの境界、エラーの扱い、許容する誤差、検証の例を手順書で
初めて決めることはしません。それらは brainstorm に戻します。

### 人が判断する場面の宣言

仕様書で「ここは人が判断する」と決まっている手順には、人向けの説明に加えて、
implement が読む宣言を同じ手順の中に置きます。

````markdown
**Human gates:**

```json
{
  "version": 1,
  "gates": [
    {
      "gate_id": "profile-wording",
      "sections": ["Review profile"],
      "criterion": "観点の文言が承認した仕様の意味と一致しているか",
      "target": { "kind": "files", "paths": ["skills/ba0918-review/references/profile/default.md"] },
      "timing": "before_commit",
      "allowed_results": ["approved", "rejected"]
    }
  ]
}
```
````

- `gate_id` は手順書の中で一意
- `sections` は、この手順が基づく仕様書の節の名前
- `criterion` は、人が「はい / いいえ」で答えられる 1 つの問い
- `target` は、判断の対象。リポジトリ内の相対パスの集合（`files`）か、変わらない証拠の
  指紋（`event` と `content_identity`）のどちらか
- `timing` は `before_edit`（編集の前）、`before_commit`（コミットの前）、
  `before_implementation_green`（全手順完了の前）のどれか
- `allowed_results` は `approved` と `rejected` だけ

答えによって作る物の意味が変わるような判断は、ここに書きません。それは brainstorm で
決めることです。人の判断が要らない手順には、この宣言を書きません。

## 草稿と承認

手順書は、brainstorm の仕様書と同じ手順で正本になります。

1. AI が草稿を `.agents/tmp/plans/<手順書 ID>_<名前>_r<版>_draft.md` に保存する。
   補助スクリプト（`scripts/plan_artifact.py draft`）が保存し、パスと指紋を返す
2. AI はチャットに、草稿のパス、指紋、正本の置き場所、人が判断すべき点だけを出す。
   草稿の全文をチャットに貼らない。要約を承認の対象にしない
3. 人が自分の道具で草稿を読み、「このパスのこのファイルを、この指紋のまま正本にして
   よい」と明示する
4. AI が補助スクリプト（`scripts/plan_artifact.py publish`）で正本の場所へ移し、索引に
   登録する。移す前に指紋が同じであることを確かめ、移したあと読み戻して一致したときだけ
   登録する

黙っていること、「もう少し話そう」と言うことは承認ではありません。直したいところがあれば
対話で受けて新しい草稿を出します。人にファイルを直接編集してもらうことはしません
（編集すると指紋が変わり、古い草稿として拒否されます）。

承認の前に、正本の手順書、索引、状態ファイル、セッションの履歴を作ったり書き換えたり
しません。草稿が触ってよいのは、自分の一時ファイルだけです。

同じ名前の草稿がすでにあるとき（前の会話が途中で切れたときなど）は、置き換える対象の
指紋を明示したときだけ置き換えます。指紋が合わない古い草稿を黙って消すことはしません。

## 手順書の改訂

正本になった手順書は、その場で書き換えません。進捗の記録のために書き換えることも
しません。

- 作る物の意味、禁止、許容する差異、人が判断する場面を変える必要が出たら、手順書を
  直さずに brainstorm へ戻します。仕様書が改訂されたら、それに基づく手順書は新しい版として
  作り直します
- 意味を変えずに手順だけを直す場合（たとえば、implement が読めない書式を直す）は、
  新しい版を新しいパスに作り、全文を人に見せて同じ承認を取ります。古い版に結び付いた
  実装の証拠は古いものになります

## 未完了の手順書の索引

`.agents/artifacts/plans/open-plans.json` は、まだ終わっていない手順書の一覧です。
次だけを持ちます。

- 手順書の ID
- パス
- 版と指紋
- 「いま対象」（current）か「保留」（held）か

工程の進み具合、完了したかどうか、証拠の中身、要約は持ちません。進捗は、implement が
残した証拠から導くものであって、索引や手順書本文に手で書くものではありません。
`status.md` や `session-history.md` のような状態ファイルは作りません。

索引が壊れているとき（中身がおかしい、指すファイルの指紋が違う、「いま対象」が 2 つある）
は、AI が推測で直さずに止まります。

## いま対象の手順書を切り替えるとき

普段、「いま対象」の手順書は 1 つです。終わっていない手順書は、いくつあっても「保留」と
して残せます。

新しい手順書を「いま対象」にしようとしたとき、すでに別の手順書が「いま対象」なら、
AI は両方の目的を見せて、「古いほうは保留として残る」と説明し、人の明示の確認を取ります。
黙って入れ替えたり、古いほうを「放棄」や「完了」にしたりはしません。人がいない自動実行
でも同じです。

人が確認しても、作業ツリーに未コミットの変更があって、それが「いま対象」の手順書のもの
として回復できる形（コミット、別ブランチ、など）になっていなければ、切り替えを止めます。
その隔離は plan の仕事ではなく、implement や回復の仕組みが行います。

## やらないこと

- 仕様書に無いことを決めない。設計しない
- 実装しない。テストを書かない。コミットしない
- ブランチや作業ディレクトリ（git worktree）を作らない、切り替えない
- 進捗を手順書や状態ファイルに書かない
- 止まった実装の再開を行わない（それは implement が受け持つ）
- implement を自動で始めない

## 人が判断する場面

| 場面 | 人が決めること |
|---|---|
| 入力が足りない | brainstorm に戻って決める |
| 草稿の承認 | そのファイルを、その指紋のまま正本にしてよいか |
| 「いま対象」の切り替え | 古い手順書を保留にして切り替えてよいか |
| 未コミットの変更がある | どう隔離するか（plan は行わない） |

## 失敗したときの動き

- 正本への移動、読み戻し、索引への登録のどこかで失敗したら、どの段階で失敗したかを
  報告し、「登録した」とは言いません。草稿は再開用に残します
- 索引が壊れていたら、壊れている箇所を示して止まります

## skill の構成

```text
skills/ba0918-plan/
├── SKILL.md                 入口。責任の範囲と、どの補足をいつ読むか
├── references/
│   ├── creation.md          入力の確認、草稿の書き方、承認、正本への反映
│   ├── readability.md       手順書を人が読める形にするための確認項目
│   └── lifecycle.md         改訂、索引、いま対象の切り替え
└── scripts/
    └── plan_artifact.py     草稿の保存、正本への反映、索引の読み書き
```
